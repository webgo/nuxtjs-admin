DATABASE_URL="mysql://root:your_password@localhost:3306/admin_app"
JWT_SECRET="your-secret-key-here"

# 可选，设为 redis 连接时使用 Redis 存储（在线用户、令牌等）
# 不设则自动降级到文件系统（持久化到 .data/app_cache/）
# REDIS_URL="redis://localhost:6379"
